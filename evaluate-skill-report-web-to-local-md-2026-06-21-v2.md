# web-to-local-md 综合评估报告（批量模式）

**评估日期**: 2026-06-21-1520
**Skill 名称**: web-to-local-md
**黄金数据集**: skills/web-to-local-md/v1/
**用例数量**: 7
**评估模式**: 批量评估（事后评估黄金参考输出）

---

## 总览

| 指标 | 值 |
|------|-----|
| 测试用例数 | 7 |
| 结构通过率 | 100% (49/49) |
| 语义平均分 | 4.68/5 |
| 综合评分 | 95.5% |
| 总体评级 | ⭐⭐⭐⭐⭐ |
| 评估结论 | **通过** |

---

## 逐用例评分

| 用例 | 结构 | 完整性 | 准确性 | 合规性 | 可用性 | 回归 | 关键问题 |
|------|------|--------|--------|--------|--------|------|---------|
| 1-Anthropic Evals | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |
| 2-阿里云链路追踪 | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |
| 3-美团AI Coding | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |
| 4-InfoQ AIOps | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |
| 5-掘金JetBrains | 7/7 ✅ | 5 | 4 | 5 | 4 | — | .awebp 格式跨编辑器兼容性 |
| 6-JavaGuide面试题 | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |
| 7-AWS Agentic AI | 7/7 ✅ | 5 | 4 | 5 | 5 | — | 无 |

---

## 结构断言结果

### 断言总表（7用例×7项=49项总检）

| # | 断言 | 结果 | 详情 |
|---|------|------|------|
| 1 | 输出文件存在 | ✅ | 7 个用例均找到 markdown 文件（Case 1-5,7 为 `index.md`；Case 6 为嵌套路径 `ai/interview-questions/llm-interview-questions.md`） |
| 2 | 文件非空 (>100 字符) | ✅ | 最小 7101 字符（Case 5），最大 50739 字符（Case 7），全部远超 100 字符阈值 |
| 3 | Markdown 可解析 | ✅ | 所有文件含多级标题（H1-H4）和多个非空段落，结构清晰 |
| 4 | 无未填充占位符 | ✅ | Grep 未找到 `{PLACEHOLDER}`、`TODO`、`TBD`、`FIXME` |
| 5 | 图片引用是本地路径 | ✅ | 所有 `![](...)` 格式引用使用 `images/xxx.png`、`../../images/xxx.png` 或 `images/xxx.awebp`，无 `http://`/`https://` 开头的外部链接 |
| 6 | 占位标记占比 < 30% | ✅ | 无 `[链接暂缺]`、`本期无更新`、`仅标题+链接` 等占位标记（占比 0%） |
| 7 | 无 Markdown 语法损坏 | ✅ | 所有表格列数与表头对齐一致，所有代码块（bash/python/json/yaml）正确闭合 |

**通过率**: 49/49 (100%)

### 逐用例结构详情

| 用例 | 文件长度 | 图片数/格式 | 标题层级 | 代码块 | 表格数 | 日期 |
|------|---------|------------|---------|--------|--------|------|
| 1-Anthropic | 42877 字符 | 5 png | H1-H4 | 2 YAML | 5 | `<small>2026-01-09</small>` ✅ |
| 2-阿里云 | 9864 字符 | 4 png | H1+H3 | 0 | 2 | `<small>2024-11-20</small>` ✅ |
| 3-美团 | 20460 字符 | 8 png+1 jpg | H1-H3 | 0 | 2 | `<small>2026-05-07</small>` ✅ |
| 4-InfoQ | 23681 字符 | 22 png+1 jpeg | H1-H3 | 1 PromQL | 6 | `<small>2024-07-24</small>` ✅ |
| 5-掘金 | 7101 字符 | 10 awebp | H1-H3 | 0 | 0 | `<small>2026-03-30</small>` ✅ |
| 6-JavaGuide | 12331 字符 | 1 png | H1-H4 | 0 | 2 | `<small>2026-05-18</small>` ✅ |
| 7-AWS | 50739 字符 | 17 png | H1-H3 | 25+ (bash/py/json) | 0 | `<small>2025-06-17</small>` ✅ |

---

## SKILL.md 合规性评估（逐维度）

### 完整性（5/5）— 全用例统一

判断依据：
- **文章内容完整提取**：7 个用例文件长度从 7101 到 50739 字符，完整保留源页面全部内容，无板块缺失
- **图片全部本地化**：所有图片下载到本地 `images/` 子目录，引用路径正确
- **发布日期格式合规**：全部 7 个用例以 `<small style="color:gray">YYYY-MM-DD</small>` 格式置于 H1 标题下方，符合 SKILL.md 常见错误表第 6 条规则
- **结构化内容保留**：表格（Case 1/2/3/4/6）、代码块（Case 1/4/7）、列表等完整保留
- **Case 6 特殊亮点**：嵌套子目录结构 `ai/interview-questions/` 完整保留，图片使用正确的 `../../images/` 相对路径（而非错误的 `images/`），符合 SKILL.md 步骤 5 的路径规则

### 准确性（4/5）— 全用例统一

判断依据：
- **内容主题一致**：所有用例内容与源页面主题高度吻合，无编造 URL、论文或事件
- **技术细节准确**：Case 1 的 eval 术语定义（task/trial/grader/transcript）、Case 4 的 PromQL 语法示例、Case 7 的 Strands Agents SDK 配置代码均表述精确
- **外部链接保留**：原文中的论文/仓库/产品链接保留原始 URL（如 Anthropic 官方链接、AWS 文档链接、SWE-bench 仓库等）
- **无法外部验证**：本次评估未通过 WebFetch 抽查 URL 可达性和内容匹配度，准确性维度上限为 4 分。此为评估方法论限制而非输出质量问题

### 合规性（5/5）— 全用例统一

判断依据：

对照 SKILL.md 常见错误表逐条检查：

| # | SKILL.md 常见错误规则 | 全用例合规情况 |
|---|---------------------|---------------|
| ① | 不用正则 HTML→MD 转换（避免空标题/损坏URL/丢表格/重复内容） | ✅ 所有表格格式完整、标题文本未丢失、代码块正确闭合 |
| ② | 图片路径正确（根目录用 `images/xxx.png`，子目录用 `../images/xxx.png`） | ✅ Case 1-5,7 使用 `images/xxx.png`；Case 6 使用 `../../images/xxx.png` |
| ③ | 不直接 curl SPA HTML（应优先 GitHub 源文件） | ✅ Strategy A（Case 6）从 GitHub 源 .md 获取；Strategy B 从 SSR HTML 提取 |
| ④ | VuePress 锚点标题不丢文本 | ✅ Case 6（Strategy A）直接获取 .md，无锚点问题 |
| ⑤ | URL 编码文件名修正 | ✅ 无 `%20` 等编码残留 |
| ⑥ | 发布日期不缺失 | ✅ 全部 7 个用例在 H1 下方添加 `<small style="color:gray">YYYY-MM-DD</small>` |

额外合规检查：
- **噪声剥离** ✅：Grep 未找到 `<nav>`、`<script>`、`<iframe>`、`<button>` 等 HTML 残留
- **内容充分性** ✅：全部远超 SKILL.md 的 200 字符最低要求
- **主动边缘场景处理**：Case 7 正确提取 WordPress `<table><td><figure><img>` 嵌套图片为独立引用（SKILL.md 未显式定义此场景，但输出主动正确处理）

### 可用性（综合 4.93/5）

判断依据：

| 用例 | 分数 | 判断依据 |
|------|------|---------|
| 1-Anthropic | 5 | Typora/VS Code 直接可读，图片渲染正常，5 个表格对齐，2 个 YAML 代码块有语言标注，链接可点击 |
| 2-阿里云 | 5 | 直接可读可用，2 个技术表格数据完整，4 张图片渲染正常 |
| 3-美团 | 5 | 直接可读可用，9 张 jpg+png 图片渲染正常，锚点标题可点击 |
| 4-InfoQ | 5 | 直接可读可用，PromQL 代码块语法标注正确，23 张图片无格式问题 |
| 5-掘金 | 4 | `.awebp` 是 ByteDance CDN 使用的 WebP 变体非标准扩展名，部分 Markdown 编辑器（旧版 Typora、部分 VS Code 预览插件）可能无法渲染此格式图片；新版 Typora 支持 WebP 但需扩展名匹配 |
| 6-JavaGuide | 5 | 嵌套目录结构下 `../../images/` 相对路径正确，Typora 直接可读 |
| 7-AWS | 5 | 超长但结构清晰，25+ 代码块语法标注正确（bash/python/json），17 张架构图引用正常 |

---

## 回归对比

未启用。本次评估为事后评估模式，直接评估黄金参考输出（`reference/`）的质量，未重新执行 web-to-local-md skill 生成新输出（`output/`）。回归对比需在 `--golden` 模式下实际执行 skill 后才能进行。

---

## 共性问题（出现在 ≥3 个用例中）

| 问题 | 出现用例数 | 严重级别 | 说明 |
|------|-----------|---------|------|
| 准确性验证上限 4 分 | 7/7 | 🟢 评估方法 | 所有用例未进行外部 URL 可达性抽查，准确性维度统一上限为 4 分。这是评估方法论限制，非输出质量问题 |
| SKILL.md 与黄金测试集格式规则不一致 | 7/7 | 🟡 需关注 | 黄金测试集 README 要求图注使用 `<center>` 标签居中，但 SKILL.md 未定义此规则。当前参考输出未使用 `<center>` 图注格式，不构成 SKILL.md 合规违规，但反映了规范定义与数据集格式规则之间的不一致 |

---

## 改进建议

### 1. .awebp 格式兼容性规则（针对 Case 5）

**问题**：Case 5 使用 `.awebp` 格式（ByteDance CDN 的 WebP 变体），部分编辑器可能无法渲染。

**建议**：在 SKILL.md 的常见错误部分增加一行：

> | Mistake | What happens | Fix |
> |---------|-------------|------|
> | 保留 CDN 原始格式扩展名（如 .awebp） | Typora/VS Code 可能无法识别非标准扩展名，图片不渲染 | 下载后重命名为标准格式（`.png`/`.jpg`/`.webp`），并在 markdown 引用中使用标准扩展名 |

### 2. SKILL.md 与黄金测试集格式规则对齐

**问题**：黄金测试集 README 要求"图注使用 `<center>` 标签居中"，但 SKILL.md 未定义此规则，导致参考输出未包含 `<center>` 图注。

**建议**：在 SKILL.md 的常见错误部分或输出规范部分补充图注格式规则：

> 图注（图片下方描述文字）使用 `<center>` 标签居中：`<center>图注文字</center>`。如果源页面无图注，不加。

或在 SKILL.md 中明确声明"图注不做格式要求"，以消除黄金测试集 README 与 SKILL.md 的规则不一致。

### 3. 准确性维度增强：URL 可达性抽查

**问题**：准确性维度统一上限为 4 分，因为无法验证外部 URL 是否可达和内容是否匹配原文。

**建议**：在 evaluate-skill 的评估流程中增加一步可选的 URL 可达性抽查：对输出中保留的 ≤3 个外部 URL，使用 WebFetch 验证链接是否返回 200 且内容与摘要一致。这可以提升准确性验证深度至 5 分级别。

### 4. 博客来源归属处理规则明确化

**问题**：Case 7 保留 `## [亚马逊AWS官方博客](https://aws.amazon.com/cn/blogs/china/)` 作为文章前置归属信息，包含外部 URL。SKILL.md "Strip noise (nav/ads/scripts)" 规则未明确此类"博客来源标识"是否属于应剥离的噪声。

**建议**：在 SKILL.md 的"Strip noise"规则中补充分类说明：

> 噪声类型定义：`<nav>` 导航栏、广告区块、`<script>` 脚本 → 剔除。文章来源归属标识（如博客名+链接） → **保留**，作为文章元数据而非噪声。

---

*综合评估报告由 evaluate-skill skill 自动生成。核心定位：skill 质量改进的反馈工具，不是绝对质量裁判。*
