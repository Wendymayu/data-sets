# web-to-local-md 综合评估报告（批量模式）

**评估日期**: 2026-06-22_2034
**Skill 名称**: web-to-local-md
**黄金数据集**: D:\code\opensource\github\data-sets\skills\web-to-local-md\v1
**用例数量**: 7
**评估模式**: 批量评估（--golden 回归对比 · Skill 变更后验证）

---

## 总览

| 指标 | 值 |
|------|-----|
| 测试用例数 | 7 |
| 结构通过率 | **100%（49/49 项通过）** |
| 语义平均分 | 4.18/5 |
| 综合评分 | **88.5%** |
| 总体评级 | ⭐⭐⭐⭐ |
| 评估结论 | **通过** |

**综合评分公式**：结构通过率×0.3 + 语义平均分换算百分比×0.7 = 100%×0.3 + 83.57%×0.7 = 30.00 + 58.50 = **88.50%**

**本轮性质**：脚本于 20:20 更新（晚于 1930 轮的 19:30），新增 `normalize_tables()`（修复 case-2 表格分隔行/表头顺序倒置）与孤立尾部标题清理（修复 case-7 `## 本篇作者`）。本轮为 **Skill 变更后验证**，重点确认这两项修复是否生效。

**执行方式说明**：`--golden` 模式实际执行了目标 skill（运行核心脚本 `web_to_local_md.py`，该脚本"handles all steps automatically"）。7 个用例并行执行，所有新生成文件写入项目本地 `output/web-to-local-md/2026-06-22_2034/case-{N}/`，未污染黄金数据集目录。策略分布：case-1~5、7 走 Strategy B（HTML 转换 → `index.md`），case-6 走 Strategy A（GitHub 源文件 → 嵌套 `ai/interview-questions/llm-interview-questions.md`）。全部 82 张图片下载 0 失败（5/4/13/23/10/1/26）。

---

## 本轮修复验证结论

| 修复项 | 验证结果 | 证据 |
|--------|---------|------|
| ✅ `normalize_tables()` 修复 case-2 表格 | **通过** | case-2 的 2 张表格分隔行 `|---|` 现已正确位于表头行之后（表1第20行/表2第53行），历史 1930 轮的 🔴 Critical 断言7失败已彻底解决。结构从 48/49 → **49/49 首次满分** |
| ✅ 孤立尾部标题清理修复 case-7 | **通过** | 新输出 md 中未出现 `## 本篇作者` 孤立尾部标题及作者头像引用，源 HTML 作者头像章节已被剥离。9 张头像文件仍下载到 images/（下载先于清理），但 md 中无引用，属可接受目录冗余 |
| ✅ case-4 "内容缩水" 调查 | **误报澄清** | 下载日志"9471 字符"是 Python `len()` 字符数，而 1930 报告记载的"23124 字符"实为字节数——单位不一致造成看似缩水 60% 的假象。同口径下 new=9471 chars/23124 bytes vs ref=9638 chars/23681 bytes，仅差 167 字符（1.7%），差额完全来自 reference 更长的描述性 alt。`diff` 忽略 alt 后两文件**零差异**，正文段落/PromQL 代码块/22 图引用/标题层级全部完整无缺失 |

---

## 逐用例评分

| 用例 | 来源 | 策略 | 结构 | 完整性 | 准确性 | 合规性 | 可用性 | 语义均分 | 回归 | 关键问题 |
|------|------|------|------|--------|--------|--------|--------|---------|------|---------|
| case-1 | Anthropic 工程博客 | B | 7/7 | 5 | 4 | 5 | 4 | 4.50 | 退化2 改进1 | H1 大小写小写不符原文（evals for AI agents）；2 处 YAML 代码块缺 ```yaml 标注 |
| case-2 | 阿里云文档 | B | 7/7 | 5 | 4 | 4 | 4 | 4.25 | 改进2 退化2 | ✅ 表格修复成功；表2 部分行尾部空单元格丢弃致 3 列 vs 4 列（GFM 可渲染）；4 图 alt=image |
| case-3 | 美团技术团队 | B | 7/7 | 5 | 4 | 3 | 4 | 4.00 | 退化4 | 残留元数据"业务研发平台""算法大模型"（与 1930 一致，非新增）；8/9 图 alt=image；章节锚点链接丢失；4 张冗余图 |
| case-4 | InfoQ 中文 | B | 7/7 | 5 | 4 | 4 | 4 | 4.25 | 退化2 | ✅ "内容缩水"系误报（char/byte 单位混淆）；22 图 alt 全为 "image" |
| case-5 | 掘金 | B | 7/7 | 5 | 4 | 2 | 3 | 3.50 | 退化4 | alt=hash 文件名；文件名冗长~110 字符；作者署名块未移除；文末推广块未移除 |
| case-6 | JavaGuide（VuePress） | A | 7/7 | 5 | 5 | 5 | 5 | 5.00 | 全不变 | ✅ diff IDENTICAL，与黄金参考逐字符完全一致 |
| case-7 | AWS 中国博客 | B | 7/7 | 4 | 3 | 4 | 4 | 3.75 | 改进3 退化3 | ✅ 孤立标题"## 本篇作者"已清理；19 代码块语言标签丢失；alt=英文文件名；9 张作者头像冗余 |

---

## 逐用例详细评估

### case-1 — Anthropic 工程博客（Strategy B，en，medium）

**结构断言**：7/7 通过（100%）。文件 43769 字节/336 行，H1 + 多级 H2/H3，5 张图均为本地 `images/` 路径，3 个表格表头/分隔行/数据行齐全，2 个代码块闭合。

**合规性依据**：
- 完整性 5：Introduction、结构、为何构建、如何评估（4 类 agent）、roadmap（Step 0-8）、holistic methods、Conclusion、Acknowledgements、Appendix 全齐；含 5 图、2 YAML 示例、3 表格。
- 准确性 4：正文事实一致、日期 `2026-01-09` 匹配；但 H1 为 `# Demystifying evals for AI agents`（小写），原文/参考为 `# Demystifying Evals for AI Agents`（标题大小写），疑取自 URL slug 而非可见标题。
- 合规性 5：14 条规则全部通过。
- 可用性 4：可直接阅读；2 处 YAML 代码块缺 ```yaml 标注致无语法高亮；图片用 hash+尺寸命名（参考用语义化 `anthropic-evals-N.png`）。

**回归对比**：退化 2（H1 大小写不符、2 处 yaml 标注缺失）、改进 1（4/5 图 alt 改用 figcaption 原文描述，比参考的短语义标签更忠实）、不变 5。**与 1930 轮字节数 43769 完全相同，2 处退化与 1 处改进均原样延续，未见新增修复或退化。**

---

### case-2 — 阿里云文档（Strategy B，zh，medium）✅ 本轮修复验证点

**结构断言**：7/7 通过（100%）。文件 7537 字节，H1/H2/H3 + 段落 + 列表 + 表格 + 图片引用齐全。**两表分隔行 `|---|` 均已紧随表头之后**（表1第20行/表2第53行），历史 1930 轮的 🔴 Critical 断言7失败（分隔行置于表头前致表格无法渲染）已彻底解决。

**合规性依据**：
- 完整性 5：正文 4 节、3 个 H3 子标题、4 张图、2 张表全部保留，与 reference 内容一致。
- 准确性 4：表 1 完全正确（4 列一致）；表 2 部分数据行尾部空第 4 单元格被 sanitize 丢弃致 3 列 vs 4 列，但 GFM 渲染器自动补空，语义等价可渲染；图片 alt 用通用 `image` 而非 reference 的描述性 alt。
- 合规性 4：H1 首行 ✓ + small 日期在 H1 下方 ✓ + 只注入日期 ✓ + images/ 子目录 ✓ + **normalize_tables 已修复表头顺序 ✓**；扣分在 alt 用 image 回退未提取 figcaption 描述。
- 可用性 4：文档可读、表格可渲染、图片本地可显示。

**回归对比**：改进 2（表1表2分隔行已正确置于表头后、两表均可正常渲染）、退化 2（图片 alt 退化为 image、表2尾部空单元格丢失致列数 3 vs 4）、不变 4。**本轮 case-2 表格渲染从退化转为改进。**

---

### case-3 — 美团技术团队（Strategy B，zh，medium）

**结构断言**：7/7 通过（100%）。文件 19625 字符，标题/表格/列表/图片/引用块语法均完整正确，9 张图均为本地 `images/` 路径。

**合规性依据**：
- 完整性 5：正文、9 张内容图、5 行表格、四级章节结构均与参考一致，无缺失内容。
- 准确性 4：正文文本与参考完全一致、日期正确；但混入残留元数据"业务研发平台 2026-05-07"与"算法大模型"两行垃圾文本（参考已清理）。
- 合规性 3：残留元数据未剔除（违反只注入日期规则）；8/9 图 alt='image' 未从 figcaption/title 提取描述性 alt（违反 #17）；章节标题锚点链接全部丢失（违反 #④，参考为 `## [标题](#锚点)`，新输出为纯 `## 标题`）。
- 可用性 4：离线可读、图片本地化、章节层次清晰；alt='image' 降低可访问性，顶部垃圾元数据略显杂乱。

**图片数差异（13 vs 9）已查清**：md 实际引用的 9 张内容图与参考文件名一致；多出的 4 张（`01fab90c3e...png`/`0c57a460...png`/`6c1215ad...jpg`/`ad51e26e...jpg`）未被 md 引用，系页面头像/推广图被下载但未嵌入正文——**目录冗余，非内容退化**。

**回归对比**：退化 4（残留元数据重新出现、8 图 alt='image'、章节锚点链接丢失、4 张冗余图）、不变 13。**残留元数据问题与 1930 轮一致（1930 轮亦保留），非本轮新增退化；属持续未清理问题。**

---

### case-4 — InfoQ 中文（Strategy B，zh，hard）✅ 误报澄清

**结构断言**：7/7 通过（100%）。文件 9471 字符/23124 字节，标题/段落/列表/代码块语法均合法，22 处图引用均为本地 `images/` 路径。

**"内容缩水"调查结论：误报**。下载日志"9471 字符"是 Python `len()` 字符数，而 1930 报告记载的"23124 字符"实为字节数——单位不一致造成看似缩水 60% 的假象。同口径对比：new=9471 chars/23124 bytes vs ref=9638 chars/23681 bytes，仅差 167 字符（1.7%），差额完全来自 reference 更长的描述性 alt 文本。`diff` 忽略 alt 后两文件**零差异**，正文段落、PromQL 代码块、22 图引用、标题层级全部完整无缺失，既非站点改版也非脚本回归。

**合规性依据**：
- 完整性 5：忽略 alt 后 diff 零差异，正文段落、PromQL 代码块、22 图引用、标题层级与 reference 逐字一致。
- 准确性 4：正文准确，但 22 张图 alt 全部回退为 `![image]`，丢失 reference 的描述性 alt（如"演讲者介绍""异常检测算法分类""ARMS Insights 自动根因定位"）。
- 合规性 4：H1 首行、`<small>` 日期注入、根 `images/` 路径、无 HTML 残留均合规；违反 #17 应优先 figcaption/title 而非回退 image。
- 可用性 4：离线阅读体验良好，但图片无描述性 alt，可访问性下降。

**回归对比**：退化 2（22 图 alt 回退 image；"内容缩水"为误报非真实退化）、不变 8。**与 1930 轮状态完全一致，未修复也未新增问题。**

---

### case-5 — 掘金（Strategy B，zh，hard，.awebp）

**结构断言**：7/7 通过（100%）。文件 9318 字符/120 行，H1/列表/分隔符/粗体/图片语法均合法，10 张图全部为 `images/*.awebp` 本地路径，byteimg `.awebp` 文件名中 `:` 已正确替换为 `-`。

**合规性依据**：
- 完整性 5：正文全文保留、10 张图全部下载成功，主体内容无缺失。
- 准确性 4：正文文字准确，但 10 处图片 alt 均为原始 hash 文件名（冒号转空格），非描述性文本。
- 合规性 2：3 项违规——保留作者署名块（CodeSheep/阅读量 19835/阅读 6 分钟）、文末推广引用块（GitHub 仓库链接）未移除、alt=hash 名违反 #17（hash 名应回退 `image` 或描述性填充）；H1 首行/日期位置/byteimg sanitize 正确。
- 可用性 3：可读但含噪声——作者署名块、文末推广块、无意义 hash alt、约 110 字符冗长文件名降低可读性。

**回归对比**：退化 4（alt=hash 名、文件名冗长、作者署名块未移除、推广块未移除）、不变 7。**与 1930 轮完全一致，零改进；内容清理（作者署名、推广块、alt/hash 命名）持续未解决。**

---

### case-6 — JavaGuide AI 面试题（Strategy A，zh，easy）⭐ 最佳

**结构断言**：7/7 通过（100%）。文件 12331 字符，1 个 H1 + 8 个 H2 + 表格/列表。**新输出与黄金参考 `diff` IDENTICAL，逐字符完全一致。**

**合规性依据**：
- 完整性 5：正文、四章节、答题框架、扣分点、复习建议及图片齐全，与参考等长。
- 准确性 5：`diff` 与 reference 逐字节 IDENTICAL；嵌套两级 `ai/interview-questions/` 正确使用 `../../images/llm-token-process.png`（本用例重点）✓；H1 `# 大模型基础面试题总结` 无乱码 ✓。
- 合规性 5：H1 第一行 ✓、frontmatter 已剥离 ✓、日期 `<small>2026-05-18</small>` 紧贴 H1 下方 ✓、Strategy A 走 GitHub 源 ✓、图片名一致 ✓。
- 可用性 5：无 Copy 按钮、无 anchor span、无转义残留，可直接离线阅读。

**回归对比**：全部 6 个维度（文字/H1/图片路径/frontmatter/日期/图片数）均为**不变**。**本用例无任何退化**——Strategy A 路径质量最高，再次验证 SKILL.md「GitHub source markdown > HTML conversion」核心原则。

---

### case-7 — AWS 中国博客（Strategy B，zh，hard）✅ 本轮修复验证点

**结构断言**：7/7 通过（100%）。文件 52091 字符，38 个代码围栏（19 代码块）配对正确，17 处图片引用均为本地 `images/*.png` 路径。

**合规性依据**：
- 完整性 4：正文四章全文保留，17 张图全部引用，19 段代码示例均在；扣分在 19 个代码块语言标签丢失、图片 alt 退化为英文文件名。
- 准确性 3：正文文本与参考一致；但图片 alt 为 'Practical guide...' 英文文件名派生而非参考的中文描述（"图1 用户视角下的Agent工作流程"），且代码块语言标注全部丢失（bash/json/python）。
- 合规性 4：H1 第一行 ✓、日期在 H1 下 ✓、图片路径 `images/` ✓、**WordPress 图集表格已展开为 17 独立引用 ✓**、**孤立尾部标题已清理 ✓（本轮修复点验证通过）**、无 H1 乱码 ✓；扣分在 alt 为文件名（弱填充）。
- 可用性 4：可离线阅读，17 个显式图注（图1~图17）补偿了 alt 弱化，结构清晰；代码块无语法高亮为减分点。

**图片数差异（26 vs 17）已查清**：md 引用的 17 张架构图与参考一致；多出的 9 张（hcihang/awsdawei/chenyip/chuanxie/guoren/mlil/sijie/sunhua/tangaws.jpg）为作者头像，下载到 images 目录但未在 md 引用——**目录冗余，非内容退化**（下载先于内容清理所致）。

**回归对比**：改进 3（孤立尾部标题"## 本篇作者"已清理、H1 位于第一行优于参考、17 个显式图注保留）、退化 3（19 代码块语言标签丢失、alt=英文文件名、9 张作者头像冗余）、不变 4。**孤立尾部标题清理验证通过，本轮新增清理脚本生效。**

---

## 共性问题（出现在 ≥3 个用例中）

### 1. 图片 alt 文本退化为通用/无意义文本（出现在 5/7 用例：case-1/3/4/5/7）⚠️ 最主要持续问题

- case-1：4/5 图 alt 用 figcaption 原文（尚可，但 reference 用短语义标签）
- case-3：9 图中 8 图 alt="image"
- case-4：22 图 alt 全为 "image"
- case-5：alt 为原始 hash 文件名（冒号转空格）
- case-7：alt 为文件名衍生英文（如 `Practical guide to building agentic ai applications...`）

参考文件均提供描述性中文 alt（"阿里云端到端链路追踪架构图""异常检测算法分类""重构时间线与执行路径"等）。仅 case-6（Strategy A 源文件自带）未受影响。这是当前 skill 输出与黄金参考最大的系统性差距，且在本轮未修复。

### 2. 次要共性问题（各 2/7 用例，未达 ≥3 阈值但值得记录）

- **代码块语言标签丢失**（case-1 的 2 个 ```yaml、case-7 的 19 个 ```bash/json/python）：markdownify 转换时未保留 `<pre><code class="language-xxx">` 的语言 class，致裸 ``` 围栏无语法高亮。
- **images 目录冗余文件**（case-3 多 4 张、case-7 多 9 张作者头像）：图片下载阶段未过滤非正文图片，无关头像/推广图被下载到本地但未嵌入 md。
- **推广/作者元数据残留**（case-3 残留"业务研发平台""算法大模型"、case-5 作者署名块 + 文末推广引用块）：噪声清理未覆盖这些块。

### 3. 单点问题

- **case-2 表 2 尾部空单元格丢失**（仅此 1 例，已由 normalize_tables 修复主要问题）：表 2 部分数据行尾部空第 4 单元格被 sanitize 丢弃致 3 列 vs 4 列，GFM 可自动补空渲染，属轻微结构瑕疵非渲染阻断。

---

## 改进建议

> 建议均指向 SKILL.md 的具体位置，便于迭代优化。本轮 `normalize_tables()` 与孤立尾部标题清理两项修复已验证生效，以下为尚未解决的持续问题。

### 建议 1（针对共性问题 1，最高优先级）——强化图片 alt 生成

**建议在 SKILL.md 的「Common Mistakes」表格中扩展 #17「Empty image alt text」规则**：
- 当前规则仅要求 `enrich_image_alts()` 用 figcaption/title 填充、`fill_image_alt_text()` 对 hash 名返回 `image`。建议补充：**对中文站点（InfoQ/美团/掘金/AWS），alt 应优先从 `<figcaption>`/`title`/`alt` 属性提取描述性中文文本；当这些来源为空时，不应回退到通用 `image` 或 hash 文件名，而应保留原文 `<figcaption>` 文本或基于图片周围段落生成简短描述**。这是当前 5/7 用例退化的根因，本轮未修复。

### 建议 2（针对共性问题 2a）——保留代码块语言标注

**建议在 SKILL.md 的「Common Mistakes」表格新增一行**：
- 规则名：`代码块语言标签丢失`；现象：`<pre><code class="language-yaml">` 转换后变为裸 ``` 围栏，无语法高亮；修复：`clean_html_artifacts()` 或 markdownify 转换前，提取 `<code>` 的 `language-*` class，注入为围栏语言标注（```yaml/```bash/```python）。影响 case-1（2 块）、case-7（19 块）。

### 建议 3（针对共性问题 2b）——过滤非正文图片

**建议在 SKILL.md 的「Implementation」步骤 4（Download images）中补充过滤规则**：
- 当前 `all_remote_images` 收集了页面所有 `<img>`，包括作者头像、推广图。建议：**仅下载正文内容区（`<article>`/`<main>`/`figure`）内、且被 md 实际引用的图片；对作者头像（常见于 `<figure class="author-avatar">` 或 `.jpg` 姓名型文件名）、推广 banner 在下载阶段过滤**。可消除 case-3（4 张）、case-7（9 张）的目录冗余。

### 建议 4（针对共性问题 2c）——清理推广/元数据内容

**建议在 SKILL.md 的「Common Mistakes」表格新增一行**：
- 规则名：`推广引用块与作者元数据残留`；现象：文末 GitHub 仓库推广引用块、作者署名/阅读量/阅读时长等元数据、以及页面顶部无主元数据（如"业务研发平台""算法大模型"）未被 `clean_html_artifacts()` 清理；修复：在 `NOISE_CLASSES`/噪声选择器中增加推广引用块（`.promotion`/文末 CTA）、作者元数据块、页面顶部无主标签行的识别与剥离。影响 case-3、case-5。

### 建议 5（针对 case-2 单点问题）——补全表格空单元格

**建议在 SKILL.md 的「Common Mistakes」表格 #12 `normalize_tables()` 行补充**：
- 当前 `normalize_tables()` 已正确把首行 `<td>` 提升为 `<th>` 修复表头/分隔行顺序（本轮验证通过）；建议补充：**转换后对各数据行做列数对齐校验，尾部空单元格应保留为空 `| |` 而非丢弃，保证各行列数与表头一致**。影响 case-2 表 2（3 列 vs 4 列，GFM 可渲染但与 reference 列结构不一致）。

---

## 可重复性说明（vs 1930 轮）

| 指标 | 1930 轮 | 2034 轮（本轮） | 变化 |
|------|---------|-----------------|------|
| 结构通过率 | 97.96%（48/49） | **100%（49/49）** | +1 项（case-2 表格修复） |
| 语义平均分 | 4.21/5 | 4.18/5 | -0.03（LLM-as-Judge ±1 方差） |
| 综合评分 | 88.4% | **88.5%** | +0.2pp |
| 星级 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 持平 |

- **结构提升是实质性的**：case-2 的 🔴 Critical 断言7失败（表格分隔行置于表头前）经 `normalize_tables()` 修复后通过，结构首次达到 49/49 满分。
- **语义分微降属 LLM-as-Judge 方差**：case-3 合规性 4→3、case-4 准确性 5→4、case-5 合规性 3→2，均是对同一持续问题（alt 退化、元数据残留）的评分波动，非真实退化。依据 SKILL.md 局限性说明"同一份输出跑两次，分数可能有 ±1 波动"，属正常范围。
- **两项脚本修复均验证生效**，且发现并纠正了 1930 报告中 case-4"23124 字符"的 char/byte 单位错误。

---

## 结论

**总体评级 ⭐⭐⭐⭐（综合 88.5%，通过）**。本轮为 Skill 变更后验证，核心结论：

- ✅ **两项脚本修复全部验证通过**：`normalize_tables()` 修复 case-2 表格（结构从 48/49 → **49/49 首次满分**），孤立尾部标题清理修复 case-7 `## 本篇作者`。
- ✅ **纠正一处历史误报**：case-4 "内容缩水 60%" 实为 char/byte 单位混淆，`diff` 忽略 alt 后零差异，正文完整。
- ✅ **Strategy A 路径持续完美**：case-6 与黄金参考 `diff` IDENTICAL，逐字符一致，再次验证"GitHub source > HTML conversion"核心原则。
- ✅ **核心转换质量稳定**：7/7 用例结构断言全过（49/49），H1/日期/图片本地化/路径规则/WordPress 表格展开/byteimg sanitize 等硬规则均正确执行，全部图片下载 0 失败。
- ⚠️ **主要短板仍是图片 alt 质量**（5/7 用例退化）——本轮未修复，是当前与黄金参考最大的系统性差距，建议优先按建议 1 优化 `enrich_image_alts()`/`fill_image_alt_text()` 对中文站点的处理。
- ⚠️ **代码块语言标签丢失**（case-1/7）、**冗余图片下载**（case-3/7）、**推广/元数据残留**（case-3/5）是次要但明确的持续退化点，本轮均未修复。

整体而言 skill 可直接用于离线阅读场景，本轮两项修复提升了结构稳健性。若要追平黄金参考的描述性 alt 与代码高亮质量，需按上述 5 条建议继续迭代 SKILL.md 与脚本。

---

*综合评估报告由 evaluate-skill skill 自动生成。核心定位：skill 质量改进的反馈工具，不是绝对质量裁判。*
