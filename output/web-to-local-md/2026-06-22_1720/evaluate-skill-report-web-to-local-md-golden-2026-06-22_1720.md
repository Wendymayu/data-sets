# web-to-local-md 综合评估报告（批量模式）

**评估日期**: 2026-06-22_1720
**Skill 名称**: web-to-local-md
**黄金数据集**: D:\code\opensource\github\data-sets\skills\web-to-local-md\v1
**用例数量**: 7
**评估模式**: 批量评估（--golden 回归对比）

---

## 总览

| 指标 | 值 |
|------|-----|
| 测试用例数 | 7 |
| 结构通过率 | 97.96%（48/49 项通过） |
| 语义平均分 | 4.18/5 |
| 综合评分 | 87.9% |
| 总体评级 | ⭐⭐⭐⭐ |
| 评估结论 | **通过** |

**综合评分公式**：结构通过率×0.3 + 语义平均分换算百分比×0.7 = 97.96%×0.3 + 83.6%×0.7 = 29.39 + 58.50 = **87.89%**

**执行方式说明**：`--golden` 模式实际执行了目标 skill（按 web-to-local-md 的 SKILL.md「Implementation」章节运行核心脚本 `web_to_local_md.py`，该脚本"handles all steps automatically"）。7 个用例并行执行，所有新生成文件写入项目本地 `output/web-to-local-md/2026-06-22_1720/case-{N}/`，未污染黄金数据集目录。策略分布：case-1~5、7 走 Strategy B（HTML 转换 → `index.md`），case-6 走 Strategy A（GitHub 源文件 → 嵌套 `ai/interview-questions/llm-interview-questions.md`）。全部图片下载 0 失败。

---

## 逐用例评分

| 用例 | 来源 | 策略 | 结构 | 完整性 | 准确性 | 合规性 | 可用性 | 语义均分 | 回归 | 关键问题 |
|------|------|------|------|--------|--------|--------|--------|---------|------|---------|
| case-1 | Anthropic 工程博客 | B | 7/7 | 5 | 4 | 5 | 4 | 4.50 | 退化2 改进1 | 代码块缺 `yaml` 语言标注；H1 标题大小写不符原文 |
| case-2 | 阿里云文档 | B | 6/7 | 4 | 3 | 4 | 3 | 3.50 | 退化3 改进1 | 2 张表格语法损坏（分隔行在表头前、列数不一致）；alt="image" |
| case-3 | 美团技术团队 | B | 7/7 | 5 | 5 | 4 | 4 | 4.50 | 退化3 改进1 | 8/9 图 alt="image"；4 张未引用冗余图片（头像/推广图） |
| case-4 | InfoQ 中文 | B | 7/7 | 5 | 5 | 4 | 4 | 4.50 | 退化1 | 22 张图 alt 全为 "image" |
| case-5 | 掘金 | B | 7/7 | 4 | 4 | 3 | 3 | 3.50 | 退化4 | alt=hash 文件名；文末推广引用块未移除；图片文件名冗长 |
| case-6 | JavaGuide（VuePress） | A | 7/7 | 5 | 5 | 5 | 5 | 5.00 | 全不变 | 无——与黄金参考逐字符完全一致（diff IDENTICAL） |
| case-7 | AWS 中国博客 | B | 7/7 | 4 | 3 | 4 | 4 | 3.75 | 退化3 改进1 | 25 个代码块缺语言标注；9 张作者头像冗余；alt=文件名英文 |

---

## 逐用例详细评估

### case-1 — Anthropic 工程博客（Strategy B，en，medium）

**结构断言**：7/7 通过（100%）。文件 43769 字符，H1 + 多段落，5 张图均为本地 `images/` 路径，3 个表格列对齐正确、2 个代码块闭合。

**合规性依据**：
- 完整性 5：H1、发布日期 `<small style="color:gray">2026-01-09</small>`（H1 下方）、全部章节、5 图 alt、2 个 YAML 代码示例、致谢附录齐全。
- 准确性 4：正文事实一致、日期与参考匹配；但 H1 为 `# Demystifying evals for AI agents`（句首大写），原文/参考为 `# Demystifying Evals for AI Agents`（标题大写）；2 处代码块用裸 ``` 而非 ```yaml。
- 合规性 5：12 条常见错误规则全部通过。
- 可用性 4：可离线阅读；代码块缺 yaml 标注致无语法高亮，图片用 hash 文件名（参考用语义化 `anthropic-evals-N.png`）。

**回归对比**：退化 2（代码块 yaml 标注缺失、H1 大小写不符）、改进 1（图片 4/5 的 alt 比参考更贴合 caption）、不变 2。

---

### case-2 — 阿里云文档（Strategy B，zh，medium）

**结构断言**：6/7 通过（85.7%）。🔴 断言 7 失败：**两张表格的 `|---|` 分隔行出现在表头行之前**（应为表头在前），且第二张表 6 行仅 3 列（表头为 4 列），导致表格在主流渲染器无法正确渲染。

**合规性依据**：
- 完整性 4：H1、引言、3 个 H3 子章节、2 表格全部数据行、4 图引用齐全。
- 准确性 3：表格分隔行/表头顺序倒置 + 列数不一致致"支持协议"列数据错位；日期 2024-11-20 与 README 一致（参考为 2025-02-11，疑为更新日期）。
- 合规性 4：12 条规则基本达标，但表格语法损坏是边缘违规。
- 可用性 3：表格渲染错乱；4 图 alt 均为通用 `image`（参考有"阿里云端到端链路追踪架构图"等描述性 alt）。

**回归对比**：退化 3（表格分隔行/表头顺序倒置、第二表列数不足、表格无对齐填充、alt 退化为 image）、改进 1（日期更正）、不变 1。

---

### case-3 — 美团技术团队（Strategy B，zh，medium）

**结构断言**：7/7 通过（100%）。文件 7983 字符，4 个 H2 + H3，Step1-5 表格 5 列对齐正常。

**合规性依据**：
- 完整性 5：四大章节、全部正文、经验一/二/三点、Step1-5 表格、9 张内容图全引用。
- 准确性 5：正文与参考逐段一致；正确剔除了参考中残留的"业务研发平台 2026-05-07""算法大模型"两行无主元数据。
- 合规性 4：日期/路径/文件名/H1 均达标；扣分在 9 张图 8 张用泛化 alt="image"（参考有"重构时间线与执行路径"等描述性 alt）。
- 可用性 4：结构清晰；泛化 alt 削弱可访问性；images 目录多 4 个未引用文件（疑似头像/推广图）造成冗余。

**图片数差异（13 vs 9）已查清**：md 实际引用的 9 张内容图与参考文件名完全一致；多出的 4 个文件（`6c1215addf...jpg`、`01fab90c3e...png`、`0c57a460...png`、`ad51e26e...jpg`）未被 md 引用，系页面头像/推广图被下载但未嵌入正文——**目录冗余，非内容退化**。

**回归对比**：退化 3（8 张图 alt="image"、4 张未引用冗余文件、章节标题丢失锚点链接）、改进 1（剔除残留元数据）、不变 1。

---

### case-4 — InfoQ 中文（Strategy B，zh，hard）

**结构断言**：7/7 通过（100%）。文件 23124 字符，H1 + 有序列表 + PromQL 代码块闭合，22 处图引用均本地路径。

**合规性依据**：
- 完整性 5：H1、日期 2024-07-24、AIOps→LLM 转 PromQL 全文、PromQL 代码块、RAG 选型/CoT 有序列表、22 图引用齐全。
- 准确性 5：H1、日期、正文、代码块、22 图文件名与参考逐一比对完全一致。
- 合规性 4：硬规则全过；扣分在 **22 处图 alt 全为 "image"**（参考有"演讲者介绍""异常检测算法分类""ARMS Insights 自动根因定位"等描述性 alt）。
- 可用性 4：结构清晰；泛化 alt 降低无障碍/SEO 价值。

**回归对比**：退化 1（22 处 alt 退化），其余（H1/日期/正文/代码块/图文件名）全不变。

---

### case-5 — 掘金（Strategy B，zh，hard，.awebp）

**结构断言**：7/7 通过（100%）。文件 9318 字符，10 图引用均本地 `images/`，byteimg `.awebp` 文件名中 `:` 已正确替换为 `-`。

**合规性依据**：
- 完整性 4：正文完整保留；但额外保留了作者署名块（CodeSheep/阅读量 19835/阅读 6 分钟）及文末推广引用块（GitHub 仓库链接），参考已移除。
- 准确性 4：文字无乱码、日期 2026-03-30 正确、.awebp 文件名 sanitize 正确；但图片 alt 为原始文件名（冒号转空格），如 `8f35ba86a0c6406aa1973cdb9f7383a4~tplv 73owjymdk6 jj mark...`，缺乏语义。
- 合规性 3：硬规则基本满足；缺陷：推广引用块未移除、alt 为无意义文件名、作者元数据未清理。
- 可用性 3：alt 为无意义 hash 片段严重影响可访问性；文件名约 110 字符冗长难辨识；推广内容降低阅读体验。

**回归对比**：退化 4（alt=文件名、文件名冗长、推广块未移除、作者元数据未清理），不变 5（H1/日期/正文/图片数/粗体）。

---

### case-6 — JavaGuide AI 面试题（Strategy A，zh，easy）⭐ 最佳

**结构断言**：7/7 通过（100%）。文件 12331 字符，1 个 H1 + 8 个 H2 + 表格/列表。**新输出与黄金参考逐字符完全一致（diff IDENTICAL）**。

**合规性依据**：
- 完整性 5：8 个 H2 章节全保留，与参考逐字符一致。
- 准确性 5：文字/表格/图片 alt/参考链接/日期均准确，H1 `# 大模型基础面试题总结` 无乱码。
- 合规性 5：Strategy A 优先 GitHub 源 ✓；**嵌套两级 `ai/interview-questions/` 正确使用 `../../images/llm-token-process.png`**（本用例重点）✓；日期 `<small style="color:gray">2026-05-18</small>` 在 H1 下方 ✓；frontmatter 已剥离 ✓。
- 可用性 5：Markdown 干净可读，结构层次清晰，图片相对路径正确可本地渲染。

**回归对比**：全部 6 个维度（文字/H1/图片路径/frontmatter/日期/图片数）均为**不变**。**本用例无任何退化**——Strategy A 路径质量最高，验证了 SKILL.md「GitHub source markdown > HTML conversion」的核心原则。

---

### case-7 — AWS 中国博客（Strategy B，zh，hard）

**结构断言**：7/7 通过（100%）。文件 52110 字符，25 个代码块（50 个围栏标记）全部成对闭合，17 图引用均本地路径。

**合规性依据**：
- 完整性 4：4 大章节、17 张架构图、25 代码块完整保留；"本篇作者"章节仅标题无内容（与参考一致）。
- 准确性 3：文字准确；但 **25 个代码块全部丢失语言标签**（参考正确标注 ```bash/```json/```python，新输出全为裸 ```）。
- 合规性 4：12 项全过；**重点：WordPress 嵌套 `<table><td><figure><img>` 已正确展开为 17 个独立图片引用，无空 `|---|` 占位** ✓。但 9 张作者头像 .jpg 被下载却未引用，属冗余。
- 可用性 4：新增 17 个显式图注（"图 1. 用户视角下的 Agent 工作流程"等，**对参考的改进**）；但代码块缺语言标签影响高亮，alt 为文件名衍生英文。

**图片数差异（26 vs 17）已查清**：md 引用的 17 张架构图与参考一致；多出的 9 张（`hcihang.jpg`/`awsdawei.jpg`/`chenyip.jpg`/`chuanxie.jpg`/`guoren.jpg`/`mlil.jpg`/`sijie.jpg`/`sunhua.jpg`/`tangaws.jpg`）均为作者头像，下载到 images 目录但未在 md 中引用——**目录冗余，非内容退化**。

**回归对比**：退化 3（25 代码块语言标签丢失、alt=文件名英文、9 张作者头像冗余）、改进 1（新增 17 个显式图注）、不变 4。

---

## 共性问题（出现在 ≥3 个用例中）

### 1. 图片 alt 文本退化为通用/无意义文本（出现在 5/7 用例：case-2/3/4/5/7）⚠️ 最主要问题

- case-2：4 图 alt 全为 `image`
- case-3：9 图中 8 图 alt 为 `image`
- case-4：22 图 alt 全为 `image`
- case-5：alt 为原始 hash 文件名（冒号转空格）
- case-7：alt 为文件名衍生英文（如 `Practical guide to building agentic ai applications...`）

参考文件均提供描述性中文 alt（"阿里云端到端链路追踪架构图""异常检测算法分类""重构时间线与执行路径"等）。仅 case-1（英文，alt 尚可）和 case-6（Strategy A 源文件自带）未受影响。这是当前 skill 输出与黄金参考最大的系统性差距。

### 2. 次要共性问题（各 2/7 用例，未达 ≥3 阈值但值得记录）

- **代码块语言标签丢失**（case-1 的 2 个 ```yaml、case-7 的 25 个 ```bash/json/python）：markdownify 转换时未保留 `<pre><code class="language-xxx">` 的语言 class，致裸 ``` 围栏无语法高亮。
- **images 目录冗余文件**（case-3 多 4 张、case-7 多 9 张作者头像）：图片下载阶段未过滤非正文图片，无关头像/推广图被下载到本地但未嵌入 md。

### 3. 单点严重问题

- **case-2 表格语法损坏**（仅此 1 例，但属 🔴 Critical 级断言失败）：markdownify 表格转换时分隔行与表头顺序倒置 + 列数不一致，致 2 张表格无法渲染。

---

## 改进建议

> 建议均指向 SKILL.md 的具体位置，便于迭代优化。

### 建议 1（针对共性问题 1，最高优先级）——强化图片 alt 生成

**建议在 SKILL.md 的「Common Mistakes」表格中扩展"Empty image alt text"规则**：
- 当前规则仅要求 `enrich_image_alts()` 用 figcaption/title 填充、`fill_image_alt_text()` 对 hash 名返回 `image`。建议补充：**对中文站点（InfoQ/美团/掘金/AWS），alt 应优先从 `<figcaption>`/`title`/`alt` 属性提取描述性中文文本；当这些来源为空时，不应回退到通用 `image` 或 hash 文件名，而应保留原文 `<figcaption>` 文本或基于图片周围段落生成简短描述**。这是当前 5/7 用例退化的根因。

### 建议 2（针对共性问题 2a）——保留代码块语言标注

**建议在 SKILL.md 的「Common Mistakes」表格新增一行**：
- 规则名：`代码块语言标签丢失`；现象：`<pre><code class="language-yaml">` 转换后变为裸 ``` 围栏，无语法高亮；修复：`clean_html_artifacts()` 或 markdownify 转换前，提取 `<code>` 的 `language-*` class，注入为围栏语言标注（```yaml/```bash/```python）。影响 case-1（2 块）、case-7（25 块）。

### 建议 3（针对共性问题 2b）——过滤非正文图片

**建议在 SKILL.md 的「Implementation」步骤 4（Download images）中补充过滤规则**：
- 当前 `all_remote_images` 收集了页面所有 `<img>`，包括作者头像、推广图。建议：**仅下载正文内容区（`<article>`/`<main>`/`figure`）内、且被 md 实际引用的图片；对作者头像（常见于 `<figure class="author-avatar">` 或 `.jpg` 姓名型文件名）、推广 banner 在下载阶段过滤**。可消除 case-3（4 张）、case-7（9 张）的目录冗余。

### 建议 4（针对 case-2 单点问题）——修复表格转换语法

**建议在 SKILL.md 的「Common Mistakes」表格新增一行**：
- 规则名：`Markdown 表格分隔行/表头顺序倒置`；现象：markdownify 转换 HTML `<table>` 时 `|---|` 分隔行出现在表头行之前，且部分行列数不足致渲染错乱；修复：`convert_image_tables()`/表格转换逻辑需保证 **表头行在前、分隔行紧随其后、各数据行列数与表头一致（不足补空单元格）**。影响 case-2（2 张表）。

### 建议 5（针对 case-5）——清理推广/元数据内容

**建议在 SKILL.md 的「Common Mistakes」表格新增一行**：
- 规则名：`推广引用块与作者元数据残留`；现象：文末 GitHub 仓库推广引用块、作者署名/阅读量/阅读时长等元数据未被 `clean_html_artifacts()` 清理；修复：在 `NOISE_CLASSES`/噪声选择器中增加推广引用块（`.promotion`/文末 CTA）与作者元数据块的识别与剥离。影响 case-5。

---

## 结论

**总体评级 ⭐⭐⭐⭐（综合 87.9%，通过）**。web-to-local-md 在 7 个黄金用例上整体表现优质：

- ✅ **核心转换质量稳定**：7/7 用例结构断言几乎全过（48/49），H1/日期/图片本地化/路径规则/WordPress 表格展开等硬规则均正确执行，全部图片下载 0 失败。
- ✅ **Strategy A 路径完美**：case-6 与黄金参考逐字符一致，验证了"GitHub source > HTML conversion"的核心原则。
- ⚠️ **主要短板是图片 alt 质量**（5/7 用例退化）——这是当前与黄金参考最大的系统性差距，建议优先按建议 1 优化 `enrich_image_alts()`/`fill_image_alt_text()` 对中文站点的处理。
- ⚠️ **代码块语言标签丢失**（case-1/7）和**冗余图片下载**（case-3/7）是次要但明确的退化点。
- 🔴 **case-2 表格语法损坏**是唯一的 Critical 级断言失败，需按建议 4 修复表格转换逻辑。

整体而言 skill 可直接用于离线阅读场景，但若要追平黄金参考的描述性 alt 与代码高亮质量，需按上述 5 条建议迭代 SKILL.md 与脚本。

---

*综合评估报告由 evaluate-skill skill 自动生成。核心定位：skill 质量改进的反馈工具，不是绝对质量裁判。*
