# 复制为 .env 并填入真实值。.env 已被 gitignore，不会提交。
# 优先级：CLI flag > 已有 shell 环境变量 > 本文件 > 默认。

OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL_NAME=gpt-4o-mini
TEMPERATURE=0
